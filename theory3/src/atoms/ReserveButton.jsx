const ReserveButton = ({ children }) => {
  return (
    <button className="bg-blue-600 text-white p-7 w-fit rounded-md">
      {children}
    </button>
  );
};

export default ReserveButton;
